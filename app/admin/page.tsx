import Link from "next/link";
import { redirect } from "next/navigation";
import { getAdminSession } from "@/lib/auth";
import { Post, query } from "@/lib/db";
export const dynamic='force-dynamic';
export default async function AdminPage(){const session=await getAdminSession();if(!session)redirect('/admin/login');const {rows}=await query<Post>('select * from posts order by sort_order asc, updated_at desc');return <main className="min-h-screen bg-slate-100"><header className="border-b bg-white"><div className="mx-auto flex max-w-6xl items-center justify-between p-5"><div><h1 className="text-2xl font-black">Quản trị bài viết</h1><p className="text-sm text-slate-500">Xin chào, {session.username}</p></div><div className="flex gap-3"><Link href="/" className="rounded-xl bg-slate-100 px-4 py-2 font-semibold">Xem website</Link><form action="/api/auth/logout" method="post"><button className="rounded-xl bg-slate-900 px-4 py-2 font-semibold text-white">Đăng xuất</button></form></div></div></header><div className="mx-auto max-w-6xl p-5"><div className="mb-6 flex items-center justify-between"><h2 className="text-xl font-bold">Danh sách bài viết ({rows.length})</h2><Link href="/admin/posts/new" className="rounded-xl bg-sky-600 px-5 py-3 font-bold text-white">+ Thêm bài viết</Link></div><div className="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table className="w-full text-left"><thead className="bg-slate-50 text-sm"><tr><th className="p-4">Tiêu đề</th><th className="p-4">Trạng thái</th><th className="hidden p-4 sm:table-cell">Cập nhật</th><th className="p-4"></th></tr></thead><tbody>{rows.map(p=><tr key={p.id} className="border-t"><td className="p-4"><p className="font-bold">{p.title}</p><p className="text-sm text-slate-500">/bai-viet/{p.slug}</p></td><td className="p-4"><span className={p.status==='published'?'rounded-full bg-green-100 px-3 py-1 text-xs font-bold text-green-700':'rounded-full bg-amber-100 px-3 py-1 text-xs font-bold text-amber-700'}>{p.status==='published'?'Đã xuất bản':'Bản nháp'}</span></td><td className="hidden p-4 text-sm text-slate-500 sm:table-cell">{new Date(p.updated_at).toLocaleString('vi-VN')}</td><td className="p-4 text-right"><Link href={`/admin/posts/${p.id}`} className="font-bold text-sky-700">Chỉnh sửa</Link></td></tr>)}</tbody></table></div></div></main>}
