import { notFound } from "next/navigation";import { Post,query } from "@/lib/db";import { SiteHeader } from "@/components/site-header";import { SiteFooter } from "@/components/site-footer";import { MarkdownContent } from "@/components/markdown-content";
export const dynamic='force-dynamic';
export default async function PostPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const {rows}=await query<Post>("select * from posts where slug=$1 and status='published' limit 1",[slug]);const post=rows[0];if(!post)notFound();return <><SiteHeader/><main className="min-h-[60vh] bg-slate-50 py-16"><div className="container-page"><div className="mx-auto max-w-3xl rounded-3xl bg-white p-7 shadow-sm ring-1 ring-slate-200 md:p-12"><p className="mb-5 text-sm font-bold uppercase tracking-wider text-sky-600">SKY MOBILE</p><MarkdownContent content={post.content}/><p className="mt-10 border-t pt-5 text-sm text-slate-500">Cập nhật: {new Date(post.updated_at).toLocaleDateString('vi-VN')}</p></div></div></main><SiteFooter/></>}
